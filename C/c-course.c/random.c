#include <stdio.h>
#include <stdlib.h>

int main() {
    int random_number;

    random_number = rand(); // Generate a random number
    printf("Random number: %d\n", random_number);

    return 0;

}