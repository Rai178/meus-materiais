#include <stdio.h>

int main()
{
    // for loop = repeats a section of code a limited amount of times
    int i;

    for(i = 10; i >= 1 ; i-=2)
    {
        printf("%d\n", i);
    }

    return 0;
}