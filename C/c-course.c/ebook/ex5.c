#include <stdio.h>

int main()
{
    int lin, col;

    printf("digite o tamanho da linha:");
    scanf("%d", &lin);

    printf("digite o tamanho da coluna:");
    scanf("%d", &col);

    int mat[lin][col];

    return 0;
}